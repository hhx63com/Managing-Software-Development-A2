所有的版本都可以在Releases里找到。
只要下载下来就行，文件名里没有HTML的就是C语言的代码，将它复制到对应编译器就行。HTML版本的文件名里说到了，修改其后缀名就行


All versions can be found in Releases.
Just download it. If there is no HTML in the file name, it is the code in C language. Just copy it to the corresponding compiler. It is mentioned in the file name of the HTML version, just modify its suffix.
